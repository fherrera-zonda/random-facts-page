# Server Configuration
PORT=3000
NODE_ENV=development

# Free hits before requiring user info (configurable)
FREE_HITS_LIMIT=3

# Optional: External API keys (if you want to use premium APIs)
# NEWS_API_KEY=your_news_api_key_here

